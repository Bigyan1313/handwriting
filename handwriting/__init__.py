"""Turn text and LaTeX into a handwritten-looking PDF.

The pieces: `spec` describes a render, `handwrite.render` performs it, `hands`
and `papers` supply the handwriting and the page to write it on, and `clean`
parses the input. `app` is the desktop editor.
"""
__version__ = '0.1.0'
